- Network-attached block storage, like a virtual Storage Area Network (SAN) disk.
- Attached to a compute instance
- Automatically replicated within an availability domain
- Data is managed as a fixed size block
	- Create a partition -> create a filesystem -> mount the filesystem
## Features
- Persistent and durable
	- Can exist without a compute instance
- **Encryption by default on rest and transit, cannot turn off**
### Read/Write Shareable
- Single disk can be shared between multiple VMs
### Resizing
- Volumes can only be expanded, not shrunk
- **Online Resizing** - expand the volume size without detaching the volume from the instance
- **Offline Resizing** - detach the volume, resize via console/API, then reatach
### Replication of Block Volumes
- Replicates data between regions asynchronously
	- Disaster Recovery
	- Migration
	- Business Expansion
### Volume Groups
- Simplifies the process to create time consistent backups of multiple volumes
	- Can restore an entire group of volumes from a volume group backup
## Tiers
### Lower Cost
- For large sequential I/O workloads (2 IOPS/GB)
	- Streaming
	- Data warehousing
### Balanced
- Random I/O workloads (60 IOPS/GB)
	- Boot disks
### Higher Performance
- Most I/O demanding workloads (75 IOPS/GB)
### Ultra High Performance
- Highest I/O demanding workloads (90-225 IOPS/GB)
	- Relational databases
- IOPS caps at 300,000 
### Auto-Tune 
#### Performance Based
- Adjusts VPUs by demand
	- VPUs (Volume Performance Units) basically dictates how many IOPS/GB and throughput/GB it can do per gigabyte of storage
#### Detached Volume
- Change volume to Lower Cost when dettached
	- Saves money when the volume is not being used
- Returns to the previous setting when attached again

## Syntax
![[Pasted image 20250727224123.png]]
## Types of Quota Policy Statement
### Set
- Sets the max number of a cloud resource that can be used for a compartment
> `set vcn quota vcn-count to 4 in compartment Production`
> `set database quota /*exadata*/ to 1 in tenancy`
> `set compute-core quota standard2-core-count to 10 in tenancy`
- `/*exadata*/` is a wildcard that means every resource from exadata
### Unset
- Resets quotas back to the default service limits
> `unset database quota /*exadata*/ in compartment Production`
### zero
- Prevents the creation of the specified resource in the specified compartment
> `zero vcn quotas in SecuritySandbox`